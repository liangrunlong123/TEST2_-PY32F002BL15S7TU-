# 实测记录

## 2026-07-28 固定时间低频序列版本测试

本记录对应提交 `f828a78`。该版本在软启动的 2.10 秒至 2.40 秒固定加入以下 50% 占空比 PWM 频率序列：

| 启动时间 | PWM频率 |
| --- | ---: |
| [2.10s, 2.14s) | 2500Hz |
| [2.14s, 2.18s) | 1200Hz |
| [2.18s, 2.22s) | 600Hz |
| [2.22s, 2.28s) | 200Hz |
| [2.28s, 2.32s) | 600Hz |
| [2.32s, 2.36s) | 1200Hz |
| [2.36s, 2.40s) | 2500Hz |

特殊频率序列结束后，原软启动曲线整体延后 300ms，并在 3.80 秒进入 40kHz 稳定运行。

在当前硬件和测试条件下：

- 20W灯管及对应变压器可以正常启动。
- 30W灯管及对应变压器可以正常启动。
- 40W灯管及对应变压器不能正常启动，后续需要单独调整启动曲线或功率级参数。

以上结果仅代表本次样机组合和测试条件。

后续版本已将固定时间低频序列改为由连续3次不低于3.55V的ADC采样触发，以上测试结论不直接代表新触发逻辑的测试结果。

## 2026-07-29 ADC反馈触发低频序列版本测试

本版本将低频PWM序列改为由反馈ADC触发：在软启动的 `[2.05s, 2.45s]` 时间窗口内，连续3次检测到最新反馈ADC值不低于3.55V时，打断高频PWM递减阶段并输出280ms低频PWM序列；低频序列结束后继续原高频递减过程，正常触发时在3.78秒进入40kHz稳定运行。

在当前硬件和测试条件下，本版本的整体启动效果不如上一固定时间低频序列版本：

- 20W灯管及对应变压器可以正确驱动并点亮。
- 30W灯管及对应变压器可以正确驱动并点亮。
- 40W灯管及对应变压器不能正确驱动，40W测试灯管无法正常点亮。

以上结论仅代表本次工程版本、样机组合和测试条件，后续40W方案仍需继续调整触发条件、启动频率曲线或功率级参数。

## 2026-07-29 3.60V触发与45kHz递减版本

本记录对应提交 `93069b7`。该版本将反馈触发门限由3.55V提高至3.60V。连续3次检测到最新反馈ADC值不低于3.60V，并且第三次检测位于软启动的 `[2.05s, 2.45s]` 时间窗口内时，打断原高频PWM递减阶段。

触发后先输出280ms低频PWM序列，随后从45kHz开始每100ms降低1kHz；从0.78秒开始固定输出40kHz，不再恢复触发前的高频递减曲线。

实测结果：该版本可以正确驱动40W变压器，并正常点亮40W测试灯管。该结果代表当前工程版本、样机组合和本次测试条件。

## 2026-07-29 300ms低频序列版本

本记录对应提交 `88e95a5`。该版本保持3.60V连续3次确认和 `[2.05s, 2.45s]` 触发窗口，将触发后的低频序列修改为：

`2500Hz(30ms) → 1600Hz(30ms) → 1000Hz(30ms) → 400Hz(30ms) → 200Hz(60ms) → 400Hz(30ms) → 1000Hz(30ms) → 1600Hz(30ms) → 2500Hz(30ms)`

低频序列结束后，从0.30秒开始依次输出45kHz、44kHz、43kHz、42kHz和41kHz，每档保持100ms；从0.80秒开始固定输出40kHz，不再恢复触发前的高频递减曲线。

在当前硬件和测试条件下：

- 可以正确驱动20W、30W和40W变压器，并点亮对应功率的灯管。
- 灯管预热结束进入点亮状态的时刻会出现闪烁，后续仍需优化点亮切换过程。

以上结论代表当前工程版本、样机组合和本次测试条件。

## 2026-07-30 280ms低频序列版本

本版本保持3.60V连续3次确认和各档频率不变，将触发窗口调整为 `[2.05s, 2.45s]`，并将200Hz阶段缩短为40ms，把后续阶段整体提前20ms：

`2500Hz(30ms) → 1600Hz(30ms) → 1000Hz(30ms) → 400Hz(30ms) → 200Hz(40ms) → 400Hz(30ms) → 1000Hz(30ms) → 1600Hz(30ms) → 2500Hz(30ms)`

低频序列总时长为280ms；从0.28秒开始依次输出45kHz、44kHz、43kHz、42kHz和41kHz，每档保持100ms；从0.78秒开始固定输出40kHz，不再恢复触发前的高频递减曲线。

该版本尚待20W、30W和40W灯管及对应变压器实测。

## 2026-07-30 BBB版本（TEXT5高压锁频）

本版本将软启动曲线调整为从75kHz开始每50ms降低500Hz，并在3.50秒到达40kHz稳定运行。

软启动递减阶段连续3次检测到最新反馈ADC值不低于4.90V时，停止继续降频，并固定输出“触发前原输出频率+5kHz”。如果高压确认恰好与50ms降频边界重合，不执行该时刻的500Hz降频。原3.60V触发窗口及低频PWM序列不再使用。

进入故障状态后输出5.7kHz；故障恢复时重新从75kHz执行软启动，同时清空4.90V连续确认计数，重新等待触发事件。

PWM已经成功启动后，如果程序进入HAL错误处理或HardFault处理，也切换到5.7kHz故障输出；PWM初始化完成前发生异常时不启动PWM。

实测结果：该版本可以正常驱动20W、30W和40W变压器，并正常点亮对应功率的灯管。以上结论代表当前工程版本、样机组合和本次测试条件。

后续扩大电路板样本复测后发现：只有1至2块电路板可以正常驱动20W、30W和40W变压器及对应功率灯管，其余电路板不能正常驱动。此前结论仅适用于最初测试样板，BBB版本尚不具备板间一致性，需要继续排查ADC触发条件、实际锁定频率和功率级器件参数离散性。

## 2026-07-30 4.40V四次确认版本

本版本将高压锁频条件由连续3次最新ADC采样不低于4.90V，调整为连续4次最新ADC采样不低于4.40V。条件满足后仍固定输出“触发前原输出频率+5kHz”，其余软启动、故障判断和故障恢复逻辑保持不变。

按5ms采样周期计算，连续4次确认从第一次采样到第四次采样跨越15ms。该版本尚待多块电路板以及20W、30W和40W负载实测。

## 2026-07-31 DDD_负10版本

本版本将故障比较基准由0.565V调整为0.560V；高压锁频条件调整为连续7次最新ADC采样不低于4.85V。条件满足后固定输出“触发前原输出频率-10kHz”，其余软启动和状态转换逻辑保持不变。

按5ms采样周期计算，连续7次确认从第一次采样到第七次采样跨越30ms。

实测结果：该版本驱动20W、30W和40W变压器及对应功率灯管时偶尔可以点亮，尚不能稳定启动。标记提示信息：DDD_负10版本（20/30/40W偶尔可以点亮）。

## 2026-08-01 DDD_负10版本_TEXT2

本版本在DDD_负10版本基础上，将高压锁频确认次数由连续7次调整为连续8次。最新ADC采样连续8次不低于4.85V时，固定输出“触发前原输出频率-10kHz”；其余逻辑保持不变。

按5ms采样周期计算，连续8次确认从第一次采样到第八次采样跨越35ms。

实测结果：该版本可以正常驱动20W、30W和40W变压器，并正常点亮对应功率的灯管。标记信息：DDD_负10版本_TEXT2。
